type News @model @auth(rules: [{allow: public}]) {
  id: ID!
  image: String
  title: String
  content: String
}

type Admin @model @auth(rules: [{allow: public}]) {
  id: ID!
  username: String
  password: String
}
 